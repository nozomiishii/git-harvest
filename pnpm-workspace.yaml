# pnpm v11 で onlyBuiltDependencies(配列)は廃止。allowBuilds(マップ)で許可を明示する。
allowBuilds:
  "@swc/core": true
  esbuild: true
  lefthook: true
  unrs-resolver: true

minimumReleaseAgeExclude:
  - "@nozomiishii/*"
